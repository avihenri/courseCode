// Javascript Algorithms And Data Structures Certification (300 hours)

// Introduction to JavaScript

    // DATA TYPES
    /*
        1. undefined
        2. null
        3. boolean
        4. string
        5. symbol
        6. number
        7. object
    */

    // VARIABLES
        // Holds data
        var myName; 
        // Names can be letters, numbers, $ or _ (in camelCase)
            // should not contain spaces or star with a number
        myName = 'Avril';

    // Remainder operator
        // 5 % 2 = 1 because Math.floor(5 / 2) = 2 (Quotient)
        // 2 * 2 = 4
        // 5 - 4 = 1 (Remainder)

        // To check if num is even or odd
            // % 2
            // 17 % 2 = 1 (17 is Odd)
            // 48 % 2 = 0 (48 is Even)

    // += operator: multiplies var by x
        var myVar = 1;
        myVar += 5;
        console.log(myVar); // Returns 6

    // -= operator: subtracts var by x
        myVar -= 5;

    // *= operator: multiplies var by x
        myVar *= 5;

    // /= operator: divides var by x
        myVar /= 5;

    // literal quotes in strings
        var sampleStr = "Alan said, \"Peter is learning JavaScript\".";
        // Output: Alan said, "Peter is learning JavaScript".

    // Escape Sequences in Strings
        // Code	Output
        // \'	single quote
        // \"	double quote
        // \\	backslash
        // \n	newline
        // \r	carriage return
        // \t	tab
        // \b	backspace
        // \f	form feed
        